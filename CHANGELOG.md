# 變更紀錄

## [0.2.0] - Step 2: 深色模式切換、待辦篩選

### 新增
- 新增深色模式切換按鈕，位於標題列右側。
- 新增淺色 / 深色模式切換功能，按鈕顯示圖示與文字：
  - 淺色時顯示 `🌙 深色模式`
  - 深色時顯示 `☀️ 淺色模式`
- 新增全部、未完成、已完成三種待辦篩選按鈕。
- 新增依篩選狀態顯示不同空白提示文字。

### 變更
- 儲存使用者主題偏好到 `localStorage`，重新整理後可維持選擇。
- 若使用者從未手動切換主題，則依照作業系統的 `prefers-color-scheme` 自動跟隨淺色或深色設定。
- 調整整體 UI 色彩為可變化的 CSS 變數設計，讓深色模式與淺色模式能共用同一套風格系統。
- 篩選功能會只顯示符合條件的待辦項目，不影響底部的整體未完成數量統計。
- 底部「未完成:N 項」維持計算全體待辦資料，篩選不會改變此數字。

---

## [0.1.0] - Step 1: 基本的新增、完成、刪除、未完成計數、localStorage 保存

### 新增
- 新增待辦事項輸入框與新增按鈕。
- 新增待辦項目可勾選完成狀態的功能。
- 新增每筆待辦右側的刪除按鈕。
- 新增未完成項目計數顯示，於底部呈現 `未完成: N 項`。
- 新增空清單提示文字：`還沒有任何待辦事項,新增一個吧!`
- 新增 `localStorage` 持久化儲存功能，讓資料在重新整理後保留。

### 變更
- 完成的待辦項目會顯示刪除線與淡化效果。
- 空白內容輸入時會被忽略，不會新增到清單中。
- 清單為空時會顯示提示狀態，避免顯示空白區塊。
- 使用純 HTML、CSS、原生 JavaScript 實作，保持離線可用與無套件依賴。
